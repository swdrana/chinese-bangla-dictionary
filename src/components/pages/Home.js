import React from 'react';
import ShowWordList from '../shared/ShowWordList';

const Home = () => {
    return (
        <div>
            <ShowWordList/>
        </div>
    );
};

export default Home;